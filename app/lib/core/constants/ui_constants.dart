/// UI-related constants including padding, spacing, and animation durations
class UIConstants {
  UIConstants._();

  // ============================================================================
  // Spacing
  // ============================================================================

  /// Extra small spacing (4.0)
  static const double spacingXs = 4.0;

  /// Small spacing (8.0)
  static const double spacingSm = 8.0;

  /// Medium spacing (16.0)
  static const double spacingMd = 16.0;

  /// Large spacing (24.0)
  static const double spacingLg = 24.0;

  /// Extra large spacing (32.0)
  static const double spacingXl = 32.0;

  /// Extra extra large spacing (48.0)
  static const double spacingXxl = 48.0;

  // ============================================================================
  // Padding
  // ============================================================================

  /// Screen horizontal padding
  static const double screenPaddingHorizontal = 16.0;

  /// Screen vertical padding
  static const double screenPaddingVertical = 16.0;

  /// Card padding
  static const double cardPadding = 16.0;

  /// Button padding
  static const double buttonPadding = 16.0;

  // ============================================================================
  // Border Radius
  // ============================================================================

  /// Small border radius (4.0)
  static const double borderRadiusSm = 4.0;

  /// Medium border radius (8.0)
  static const double borderRadiusMd = 8.0;

  /// Large border radius (12.0)
  static const double borderRadiusLg = 12.0;

  /// Extra large border radius (16.0)
  static const double borderRadiusXl = 16.0;

  // ============================================================================
  // Touch Targets
  // ============================================================================

  /// Minimum touch target size (48x48dp for accessibility)
  static const double minTouchTarget = 48.0;

  /// Icon button size
  static const double iconButtonSize = 48.0;

  // ============================================================================
  // Animation Durations
  // ============================================================================

  /// Short animation duration (150ms)
  static const Duration animationDurationShort = Duration(milliseconds: 150);

  /// Medium animation duration (300ms)
  static const Duration animationDurationMedium = Duration(milliseconds: 300);

  /// Long animation duration (500ms)
  static const Duration animationDurationLong = Duration(milliseconds: 500);

  /// Very long animation duration (1000ms)
  static const Duration animationDurationVeryLong = Duration(milliseconds: 1000);

  // ============================================================================
  // Elevation
  // ============================================================================

  /// Low elevation (1.0)
  static const double elevationLow = 1.0;

  /// Medium elevation (2.0)
  static const double elevationMedium = 2.0;

  /// High elevation (4.0)
  static const double elevationHigh = 4.0;

  // ============================================================================
  // Icon Sizes
  // ============================================================================

  /// Small icon size (16.0)
  static const double iconSizeSm = 16.0;

  /// Medium icon size (24.0)
  static const double iconSizeMd = 24.0;

  /// Large icon size (32.0)
  static const double iconSizeLg = 32.0;

  /// Extra large icon size (48.0)
  static const double iconSizeXl = 48.0;
}

